{
  "name": "focusscrum",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"SERVER,WEB\" --prefix-colors \"cyan,green\" \"pnpm --filter @focusscrum/server dev\" \"pnpm --filter @focusscrum/web start\"",
    "dev:server": "pnpm --filter @focusscrum/server dev",
    "dev:web": "pnpm --filter @focusscrum/web start",
    "build": "pnpm -r build",
    "db:push": "pnpm --filter @focusscrum/server db:push",
    "db:migrate": "pnpm --filter @focusscrum/server db:migrate",
    "db:studio": "pnpm --filter @focusscrum/server db:studio"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "prisma",
      "@prisma/client",
      "@prisma/engines",
      "esbuild"
    ]
  },
  "devDependencies": {
    "concurrently": "9.1.2",
    "prisma": "^6.6.0"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0"
  }
}
